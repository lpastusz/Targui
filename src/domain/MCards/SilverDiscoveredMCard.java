package domain.MCards;

import domain.Board;
import domain.MCard;
import domain.PlayerRepository;

/**
 *
 * @author Lukas.Pasta
 */
public class SilverDiscoveredMCard extends MCard{
    public SilverDiscoveredMCard(Board boardParam, PlayerRepository plRepositoryParam) {
        super(boardParam, plRepositoryParam);
    }  
    
    public void proceed() {
        
    }
}
